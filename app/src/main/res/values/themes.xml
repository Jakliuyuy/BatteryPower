<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.BatteryPower" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- 分区小标题：Material3 的 TitleSmall + 主色 -->
    <style name="BpSectionTitle">
        <item name="android:textAppearance">@style/TextAppearance.Material3.TitleSmall</item>
        <item name="android:textColor">?attr/colorPrimary</item>
    </style>
</resources>
